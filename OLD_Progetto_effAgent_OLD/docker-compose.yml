version: '3.8'

services:
  mqtt-broker:
    image: eclipse-mosquitto:2
    container_name: mosquitto-beta
    ports:
      - "1883:1883"
    volumes:
      - ./mosquitto.conf:/mosquitto/config/mosquitto.conf
